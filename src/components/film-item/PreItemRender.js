import FilmList from "../film-list/FilmList";
import FilmItem from "./FilmItem";

export default function PreItemRender ({results, dispatch}){

    console.log(results);
    return (
        {}
            // {
            //     state.map(value => <FilmItem key={value.id} results={value} dispatch={dispatch}/>)
            // }

    );
}
